server:
  port: 8082
  fullAddress: ${HOST:http://localhost}:${PORT:${server.port}}

spring:
  application:
    name: operador-service

  datasource:
      url: jdbc:postgresql://viaduct.proxy.rlwy.net:52205/railway
      username: postgres
      password: QIvldEimXjmXZGQSkobJxEQJeAWTPRbs
      driver-class-name: org.postgresql.Driver

  jpa:
      hibernate:
        ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
    hostname: operador-service


orderProduct:
  url: http://buscador-service/api/pelicula/%s